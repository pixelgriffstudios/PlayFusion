[package]
name = "playfusion-arcade"
version = "1.0.0"
edition = "2021"

[dependencies]
macroquad = { version = "0.4.14", features = ["audio"] }
gilrs = "0.11.0"

[profile.release]
lto = "thin"
strip = true
codegen-units = 1
